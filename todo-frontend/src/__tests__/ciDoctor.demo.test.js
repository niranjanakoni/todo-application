test('ci-doctor demo: intentional failure', () => {
	// This test intentionally fails to generate a CI failure for validating
	// the CI Doctor agentic workflow. Remove this file after testing.nnn
	expect(true).toBe(false);
});
